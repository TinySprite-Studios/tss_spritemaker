To do list for this project

- Move button, allows you to move selected areas
- undo, redo buttons (ctrl+z) (ctrl+y)
- export mode should be 2 options, single img and sprite sheet
- export grid layout, should be similar to the grid dimensions so the user can set how they export on their sprite sheet
- character limitor on renaming layers as it can go off the screen
